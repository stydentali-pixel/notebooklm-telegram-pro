{
  "name": "moataz-ai-telegram-bot",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Webhook Telegram AI bot with Vercel AI SDK, Fastify, Prisma and Railway-ready Dockerfile.",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "prisma:generate": "prisma generate",
    "prisma:push": "prisma db push",
    "postinstall": "prisma generate"
  },
  "dependencies": {
    "@ai-sdk/google": "latest",
    "@ai-sdk/groq": "latest",
    "@ai-sdk/openai": "latest",
    "@prisma/client": "6.16.3",
    "ai": "latest",
    "dotenv": "latest",
    "fastify": "latest",
    "telegraf": "latest",
    "zod": "latest"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "prisma": "6.16.3",
    "tsx": "latest",
    "typescript": "^5.6.3"
  },
  "engines": {
    "node": ">=20"
  }
}
